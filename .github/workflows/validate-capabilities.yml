name: Validate SDK Compliance

# Disabled until supabase/sdk goes public — the private-repo reusable
# workflow can't be resolved by this caller today.
# TODO: re-enable by uncommenting the pull_request trigger below.
on:
  workflow_dispatch:
  # pull_request:
  #   paths:
  #     - 'sdk-compliance.yaml'
  #     - '.github/workflows/validate-capabilities.yml'

permissions:
  contents: read

jobs:
  validate:
    permissions:
      contents: read
    uses: supabase/sdk/.github/workflows/validate-sdk-compliance.yml@main
